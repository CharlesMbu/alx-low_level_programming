#include <stdio.h>

/**
 *main - prints single digits in base 10
 *Description: Longer description of the function)?
 *Return: Always 0
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; c
	{
	if (ch == 'e' || ch == 'q')
	{
		continue;
	}
	putchar(ch);
	}
	putchar('\n');

	return (0);

}
